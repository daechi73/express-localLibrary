extends layout

block content
  h1 #{title}: #{book_instance.book}
  p= book_instance.status
  p= book_instance.due_back


  p Do you really want to delete this copy?

    form(method='POST' action='')
      div.form-group
        input#bookinstanceid.form-control(type='hidden',name='bookinstanceid', required='true', value=book_instance._id )

      button.btn.btn-primary(type='submit') Delete
